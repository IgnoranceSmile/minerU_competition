# ─── 自建 harness 运行时 ───
fastapi>=0.110
uvicorn>=0.29
sse-starlette>=2.0
python-multipart>=0.0.9          # FastAPI 文件上传
pydantic>=2.6
openai>=1.30                     # 以 OpenAI 兼容协议调用 LLM（DeepSeek 等）
httpx>=0.27                      # OpenAI client 用

# ─── 图纸处理 ───
pymupdf>=1.24                    # PDF 渲染 + Mock 解析
pillow>=10.0
pandas>=2.2                      # P2 表格 HTML → DataFrame
lxml>=4.9                        # pandas.read_html 后端
tabulate>=0.9                    # df.to_markdown
opencv-python>=4.9               # bbox 渲染（可选）

# ─── 测试 ───
pytest>=8.0

# ─── 真实 MinerU 解析（GPU 环境，可选） ───
# mineru>=3.1                    # 启用真实 MinerU 解析时安装
